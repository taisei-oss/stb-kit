# ARCHITECTURE/設計思想

ここではSTB-KITの設計思想を記載します。

STB-KITの設計上重要なドキュメントであるため、AIエージェントがこのファイルを編集することは禁止します。

このドキュメントは、STB-KITの開発者が直面した課題、思考プロセス、トレードオフの検討結果などをそのまま記録した生の開発ログです。
プロセスを残すため、敢えて整理整頓せずにそのまま記載しています。読みにくい部分もありますので、内容把握には適宜生成AIの要約等をご活用ください。

なお、本ドキュメントの内容については開発者個人、または開発チームの見解であり、組織を代表する公式見解ではありません。

## 基本的な設計方針

本プログラムは以下の基本方針を目指しています。

- 構造設計のドメイン知識を持つ人が利用、レビューしやすいようにする。多くの人がレビューしやすいように、利用側はできるだけシンプルなAPIとする
- 静的型チェックを確実に行えるようにし、意図しない不具合をできるだけ減らす
- 建築は時間軸が長いため、数年以上時間が経っても安定して使えるライブラリを目指す
- 入力は寛容に、出力は厳しく（実情として流通しているスキーマ違反ファイルは読み込むが、違反ファイルの流通をそれ以上増やさない）

詳細な設計思想を以降に記載します。

## 背景

ST-BridgeはbuildingSMART Japan 構造設計小委員会により整備されているXML形式の構造分野の情報交換仕様であり、構造解析ソフトやBIMツール間のデータ連携基盤として利用が進んでいます。

一方、実際のST-Bridgeデータは深い階層構造を持ち、実装差に起因する欠損・拡張・不整合を含むことがあるため、ST-Bridgeデータをプログラムから実装・利用する負荷が高いのが実状です。

ST-Bridgeを汎用的に扱う基盤ライブラリを整備することで、流通するST-Bridgeの安定化や、手軽にST-Bridgeデータを利用できる環境を整えることを目指します。

## 開発の経緯

C#にてST-Bridgeを扱うプログラムを2021年頃から開発しており、2025/7にSTBridge(ver2.0.2)からIFC形状を作成するツール開発コンペの開催により、C#プログラムの仕組みを利用してpython用のデータモデルおよびIFC変換機能の開発を行いました。
そのため、データモデルはv2.0以降の全バージョンに対応していますが、ST-Bridge v2.0.2を中心にコンバータなどを整備したため、コンバーターについてはv2.1.0以降に追加された要素などは今後の開発課題となります。

## 影響を受けたOSS

建築構造設計分野において、ST-Bridgeを介したデータ連携は重要な課題です。
当社では、先行OSSである「[STB-DiffChecker](https://github.com/NS-NS/STB-DiffChecker)」の取り組み（非競争領域における技術のオープン化と業界貢献）に大きく感銘を受け、STB-KITのOSS化を目指しました。

当社も先行する両社の志を継ぎ、本ツールを無償公開（OSS化）することで、建築構造分野のBIM活用の発展に寄与することを目指しています。

STB-KITには後述する差分検出機能のdiffがありますが、これは開発者のテスト用の単純なxml書式としての差分となります。
構造モデルの意味を解釈した設計的な整合チェックの比較であるSTB-DiffCheckerとは補完しあう異なる用途の機能です。

## 実装している機能

上記の通り、STB-KITはST-BridgeをIFCへつなぐプログラムとして開発されました。
その中で、必要になってきたため、下記のような機能を開発しました。

- ST-Bridgeのデータモデル: IFCへの変換処理を維持しやすく記載するため
- summary表示(現在非公開): 変換モデルの概要を確認しやすくするため
- ST-Bridgeのバージョンアップ: 各バージョンのST-Bridgeの変換に対応するため
- バリデーション: バージョンアップしたST-Bridgeが正しいST-Bridgeになっているかの確認
- ST-Bridgeの差分検出: goldenテストを行う際に、XMLの構造的に比較し、ずれた部分を把握するため
- GUIによるエディタ(現在非公開): テスト用のモデルを作りやすくするため
- 3Dビューワー(現在非公開)、PLYへの変換: 3D形状が意図通りになっているかのデバッグのため

これらの機能は、今回の目的であるIFC変換ツール以外でも再利用できそうなものが多いため、アプリケーションではなくライブラリとして整備することとしました。

## ST-Bridgeの特徴と課題

ST-Bridgeには下記のような特徴、課題がありました。これらの課題に対する一つのアプローチとしてSTB-KITを開発しています。

- 建物情報を表現するための膨大な仕様書
    - 仕様書の総ページ数 v2.1.0では666p+計算データ編389pとなり1000p以上となる
- ファイルサイズが大きい
    - 大型の建物で通常出力だけでもv2.0.2の1ファイルで50MBを超えるものもある
    - バージョンが上がるにつれ出力項目も増え、計算編なども考慮するとより大容量となっていく
- 大量の任意属性
    - 設計段階によって情報量が異なる
    - S造かRC造かで利用する情報が全く異なったりする
- 深い階層
    - BIMモデルとXMLの表記の特徴から、階層が深い
    - 梁の鉄筋など、簡単なデータを取得する場合でも、5-6階層たどるのは普通
    - v2.1.0では10階層程度たどるものもある
- 仕様として独自の拡張が認められている
    - StbExtensionを利用してスキーマ外の情報の追加が許容される
- これまでパースする標準ライブラリが存在しなかったため、出力も読み込みもXMLパーサーを利用して各自実装する必要があった
    - 車輪の再発明が多い
    - 実装差が出やすく、スキーマ違反も生じやすい

## 開発言語の選定

構造設計は1つのバグが人命に直結するため、安全性を重視しています。
また、マトリクス解析など大量の計算が必要になるため、計算速度も重要です。

上記の条件を考慮するとメモリ・型安全で高速なネイティブ言語であるRustが第一候補となります。
また、市販プログラムはC#で組まれているものも多いです。C#についてもメモリ・型安全で速度も十分に早く、連携性を考えると魅力的です。

一方で、建築士にとってRustやC#のような重量級言語は敷居が高いです。

解析などに関しては、専門家で高難度な言語を利用して実装することで、高機能が実現できます。
しかし、ST-Bridgeという膨大な仕様でデータモデルやコンバートを実装する場合、少数精鋭だとメンテナンス負荷が高まります。

そこで、扱える人が多く、多くの目でチェックでき知見を取り込めるPythonを採用しました。
Pythonは検索数や人気度で長年1位を維持しており、構造設計者でもPythonは読めるという人は多いです。

言語機能のメモリ・型安全も重要ですが、ST-Bridgeのような複雑なドメインデータを扱う上では、「実装が仕様通り正しく記述されているか」という論理的妥当性の検証が最重要です。
Pythonの採用で構造設計者が直接コードを見れる環境を作ることで、バグを最小化することが安全性につながります。

一般的にはJavaScriptも利用者が多いですが、構造業界での利用は少ないことと、重量級ファイルの扱いやすさ、データ処理ライブラリの豊富さ等を考慮しPythonを採用しました。

Pythonは自由度の高い言語で雑に書けてしまう一面もあるものの、近年は静的型チェックなども非常に充実してきており、適切に利用すれば安全なコードを書けます。また、ロジック面でも型チェックを行い、型安全性を担保します。

速度については、構造設計においてボトルネックになるのはデータ読み込み後のマトリクス解析などになります。
今回対象としているデータの読み書きや変換はO(N)-O(NlogN)であり、マトリクス解析(O(N B^2)-O(N^3))に比べると計算オーダーが小さいため、計算速度で問題になる可能性は小さいと考えます。

また、PythonはC++やRustによるネイティブ拡張が可能なため、速度が必要になった場合はその部分をネイティブ拡張を利用すればよいです。

なお、ここでいうPythonの採用というのは、中核としてまずはPythonから実装するという意味であり、将来的には多言語に展開していくことも考えます。

## 標準ライブラリ利用の原則

建築はプログラミングの世界に比べて時間の流れが遅いです。

プログラミングにおいて、3年前のライブラリはやや古い扱いですが、建築は3年前に設計した建物がまだ立っていないことは普通で、その間に設計変更をすることもあります。

建築の設計図書は建築基準法で15年間の保存が義務付けられていることを考えると、設計に利用したBIMデータも15年後も読めるようにしたいです。
（改修工事を考えると数十年前の図面を見ることもあります）

サードパーティライブラリに多く依存すると時間がたった時に依存解決に問題が発生しやすくなり、動かしにくくなる可能性があります。

そこで、多少車輪の再発明はあるかもしれませんが、依存がなくてもよい部分についてはできるだけ依存を使わずに実装します。
特に、データの読み書きを行うコア部分はパッケージを分離させ、原則依存無しとします。

これにより、開発時のPythonランタイムがあれば基本的にはデータを読める状況を保てます。

副次的に下記のような効果もあります

- 多くのライブラリを入れなくてよいので、セキュリティの厳しい会社でも導入しやすい(pipが使えなくてもホイール1つで導入可能)
- 全ての実装を追いやすく、ブラックボックスになりにくい
- 必要最小限の実装になるため、インストールに必要な容量が少ない
- 純pythonとなるため、pyodideによるWeb展開など、汎用性が高い

なお、この原則が主に適用されるのは、stbkit-coreとその利用部分に限られます。

設計変更や改修工事などの際に問題となるのは、当時のデータを読んだり、当時の検討を再現したりすることです。
そのため、コアの特にデータモデルとload部分は依存をなくす方向とします。

総合パッケージも利用apiを公開しているため、インストール自体は依存がなくても可能なようにします。（オプション依存中心）

一方で、開発時に必要なバリデーション、stbkit-codegen、stbkit-testutils、gui関係などは現在の利用が重要で、
データの読出しや再現の時に必須というわけではないため、適切に必要な依存は利用していきます。


## python対応バージョン選定

企業での利用も想定されることから、ライブラリの正式対応はセキュリティサポート内のバージョンを原則とします。

現時点では建築系のプログラムではpython3.9等も使われていますが、このライブラリを利用したコードが普及する頃には次バージョンになっていると思われるため、現バージョンに対応するために対応バージョンの下限を下げる対応は見送ります。

STB-KITのアルファ版公開の2026/9時点で、3.10はすぐにサポートが切れるため、採用を見送ります。

3.11はまだ1年程度サポートがありますが

- STB-KITはまだアルファ版で、正式版が出る頃には3.11もEOLが近い可能性が高い
- STB-KITは型付を重視しているため、3.12で採用されたタイプエイリアスやジェネリックの記法の恩恵が大きい
- 3.12はリリースから3年たち、十分枯れている
- 3.13は大きく恩恵を受ける新記法はない
- 3.14の型遅延評価デフォルトは魅力的なものの、3.14を下限にするのはまだ時期尚早

等の点から下限は3.12とすることとしました。

## パッケージ構成の設計

STB-KITは多彩な機能をもつため、全てを1つのパッケージで管理すると、頻繁なバージョン更新が生じる可能性があります。
前述の通り、データの読み込み等は、長期にわたって安定的に利用できることが理想です。
そこで、データモデル、io等のコアモジュールはcoreパッケージとして独立させます。

coreは一切の依存を禁止したいですが、スキーマチェックはコアの責務であり、ST-BridgeがXML形式である以上どうしても必要になってくるものであるため、唯一オプションでの依存とします。
stbkit-coreはST-Bridgeに関する基礎機能を持つコアパッケージ、stbkitはその他汎用機能を提供する総合パッケージとします。
コアの責務かどうかの判断は、設計判断が入るかどうかによります。機械的な処理で可能な基礎機能のみcoreに入れます。

IFC等他フォーマット変換は、どの部材を何にマッピングするかという設計判断がはいるため総合パッケージとなります。
ST-Bridgeのバージョンアップはシンプルな変換であればコアに入れるべき責務ですが、
v2.0.2からv2.1.0の更新において、多断面の表現や開口の表現の変更などで設計判断が必要となり、
実装者の判断により一意な変換とならないことから、コアパッケージから外すこととしました。

## mypyのstrict運用

Pythonでも静的型付け言語と同等の型安全性を確保するために、mypy strict運用を行います。

castやtype:ignoreは基本的に利用せずに、実行前にバグを検出できるようにします。

## データモデルの設計

### 基本方針

データモデルは抽象化した中間表現を作成するという選択肢もありますが、それをすると当社の解釈が強く出てしまいます。
ST-Bridge自体が、業界全体で作成した中間表現に近い部分もあるため、解釈を挟まず、ST-Bridgeの仕様に忠実なデータモデルとします。
そのためXMLをPythonで利用しやすいデータ構造で表現することを考えます。
json形式は、そのままclassにマッピングできますが、XMLは構成がやや複雑になります。
利便性を考え下記の方針としました。

- XMLの1つの要素は1つのclassに対応させる。
- XML属性はPythonの属性に対応(int,float,str等)。
- XMLの子要素も属性として持たせる。XMLの子要素は複数種類が複数個入れられるが、それを再現すると型ヒントが扱いにくくなるため、要素種類ごとにlistを作成。
- 子要素では最大回数が1のものが多い。毎回stb_foo[0]と書くのは冗長となるので、最大回数1のものはリストにしないことで、通常の属性と同様にアクセスできる。
- 内容についてはcontent属性として持たせる。

### バージョン管理

古いバージョンを読み込みたい場合も多いと考えられるため、データモデルは各バージョンごとに作りモジュールを分けます。
1つの意味的モデルを作ったり、常に最新モデルに変換したりする仕組みは設計判断が伴うため、先ほどの話の通りスキーマそのままを表現することを原則とします。

利便性のため総合パッケージには最新版のエイリアスなどを設けます。
最新版のモジュールが追加される際、最新版エイリアスが入れ替わるため総合パッケージは破壊的変更となりますが、コアパッケージについては追加のみのため、破壊的変更を防ぐことができ、パッケージの安定性が保てます。

### 命名規則

命名については、Pythonの標準的な命名規則に基づき、クラス名はパスカルケース、属性名はスネークケースとします。

XMLの要素名は基本的にはパスカルケースですが、一部にアンダーバー等が用いられます。
Pythonの命名規則は必要な場合アンダーバーを使うことも許容されるため、完全に1:1対応させる選択肢もあります。
一方でSTB-KITはrust等多言語展開も想定しており、アンダーバーが推奨されない言語もあります(rustでは警告が出る)。
言語間で要素名が統一されていたほうが、利用やコード生成の観点から良いと考え、Pythonのクラスも厳密なパスカルケースとしました。
そのため、アンダーバーを除く他RCやNRB等の略語は一般的に読みやすいように調整しています。

属性名については、ST-Bridgeの、属性名は座標軸の大文字やbooleanのキャメルケースが混ざりますが、上記と同様の理由から厳密なスネークケースに補正しています。
子要素の属性名は要素名をスネークケース化したものにしています。

上記のような命名規則によるケースの変更はしますが、命名については原則ST-Bridgeそのままの名称をできるだけ再現することで、利用者がST-Bridgeの知識があればSTB-KITの解釈を挟まずに直感的に利用できるようにしています。

子要素の属性は全てstb_が付き、深い階層を辿る際にやや冗長になる一面はありますが、プレフィックスを除く変更は機械的なケース変更と異なり設計判断が入るため、coreでは行うべきではないと考えられます。
また、stb_プレフィックスで子要素であることが判別しやすい、属性と子要素の名前衝突のリスクがなくなる等のメリットがあるためそのままとしています。

予約語と被った場合は_を後ろにつけます。今のところ計算編に出てくるStbCalWallSectionPropertyのAs(as)のみです。
上記の変換規則はstbkit.core._internal.name_converterモジュールにまとめているため、機械的に生成できます。

### 属性、子要素の型について

属性や子要素について、必須属性や最低回数、最大回数ともに1の要素については必須とし、その他はOptionalにするという手があります。
一方で、ST-Bridgeは大半が任意属性であり、必須とオプショナルを使い分ける利点が少ないです。
また、下記のような問題もあります

- 流通しているST-Bridgeではスキーマ通りになっていないものも多い
- コードから要素を生成する際は、インスタンスを作ってから必須属性を設定したいシチュエーションも多いです

そのため、load時の冗長性を持たせるため、全ての属性、子要素をOptionalとしました。

リストについては、XMLにおいてNoneと空リストが区別されないため、オプショナルにはせず、子要素がない場合は空のリストとします。
これにより子要素をforでまわす際にNone判定処理が不要になります(空リストならforがまわらないだけになる)

### データモデルと処理の分離

とにかく要素が多いため、読み書きなどロジックは基底クラスにまとめ、各要素は属性や子要素を持つだけにします。
参照解決やコンバートなどの複雑な機能は基本的にモジュールから分離して外部関数で実装することで、データモデルのメンテナンス性をあげます。

### コードジェネレーターによる自動実装

ST-Bridgeの800以上あるクラスを手書きで書くのはメンテナンス上よくないため、メタプログラミングを使いXSDをもとに自動生成します。
これにより、新バージョンへの早い対応が可能になります。

### データクラスによる実装(没案)

開発当初はdataclassによって実装し、バリデーションのための情報や元の要素名などはfieldのmetaで持たせる仕組みとしました。
後述の問題などでdataclassはやめ、現在は独自クラスを採用しています。

### 2つのプロパティ

前述の通り、全ての属性とリスト以外の子要素はオプショナルとなっています。
ここで問題となるのがST-Bridgeの階層の深さです。

#### 深い階層アクセス

ST-Bridgeでは下記のような値を取得したいときがそこそこあります。
~~~python
stb.stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[
    0
].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n
~~~
今回の設計により、途中のデータは全てOptionalとなります。C#には?.という演算子があるので、簡単に書けますが、pythonなので工夫が必要となります。

#### LBYLスタイル

Noneチェックを丁寧にやると下記のようになりますが、1つの鉄筋本数を取り出すだけでこの分岐はコードの保守性が下がります。
~~~python
result = None
if stb and stb.stb_model and stb.stb_model.stb_sections:
    sections = stb.stb_model.stb_sections
    if sections.stb_sec_beam_rc and len(sections.stb_sec_beam_rc) > 0:
        beam_rc = sections.stb_sec_beam_rc[0]
        if (
            beam_rc
            and beam_rc.stb_sec_bar_arrangement_beam_rc
            and len(beam_rc.stb_sec_bar_arrangement_beam_rc) > 0
        ):
            arr_beam = beam_rc.stb_sec_bar_arrangement_beam_rc[0]
            if arr_beam and arr_beam.stb_sec_bar_simple:
                simple = arr_beam.stb_sec_bar_simple
                if (
                    simple
                    and simple.stb_sec_bar_simple_main
                    and len(simple.stb_sec_bar_simple_main) > 0
                ):
                    result = simple.stb_sec_bar_simple_main[0].n
~~~

#### EAFPスタイル

読みやすさを考えるとpythonicと言われる、下記の書き方を採用したいです
~~~python
try:
    result = (
        stb.stb_model.stb_sections.stb_sec_beam_rc[0]
        .stb_sec_bar_arrangement_beam_rc[0]
        .stb_sec_bar_simple.stb_sec_bar_simple_main[0]
        .n
    )
except (AttributeError, IndexError, TypeError):
    result = None
~~~
シンプルにかけて理想的ですが、途中がOptionalであるため、mypyでエラーが生じてしまいます。
今回はmypy strict運用に耐える設計を目指しているので、これは困ります。

##### 補足

Pythonのランタイム（実行時）としてはtry-exceptでキャッチできるので正しく動きます（Pythonic）。
しかし、Mypyなどの静的型チェッカーは途中の属性がNoneかもしれない（Noneは属性を持っていない）という事実しか見ないため、
tryの中の一行に対して「Noneにはその属性はない！」と一律で赤線を引いてしまいます。

#### 属性利用のシチュエーション

値をとるシチュエーションは下記のようなものがあります。

- 読み込まれたことを前提に解析や変換などの処理を行う(Noneがあるとまずい)
- 任意属性を含むため、Noneが含まれていたら実行しない
- データを格納するため、既存データがあればそのインスタンスを利用し、なければ新たにインスタンスを作る

どの用途も頻繁に出てくるため、これらのどの用途でも書きやすくする必要があります

#### 没案1：文字列型 getメソッド
mypyエラーが出ないようにするために、引数にアクセス文字列を指定して一発で取得できるようにします。
通常表記でIDE補完を効かせて、いったんそのままアクセスする方法で記述してからその文字列をコピーペーストすることでスムーズにコーディングできます
~~~python
# 途中にNoneがあったらエラーを投げる
result = stb.get(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n"
)
# 途中にNoneがあったらNoneを返す
result = stb.get_or_none(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n"
)
# 途中にNoneがあったら、インスタンスを作っていく
result = stb.ensure_get(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n"
)
~~~

この手法でmypyエラーはなくなりますが、文字列情報でアクセスのため、型情報が消滅してしまいます。
mypyチェックを厳しく使いたいのにこれでは本末転倒です。

#### 没案2：ジェネリクスによる型指定

データ取得後もmypyチェックが効くように、引数に型を指定できるようにしてみました。
ジェネリックを使うことで、mypyに型情報を渡せます。
関数内で最終型チェックを行うことで、間違った型指定の場合は例外を出せます(castよりは安全)
~~~python
# 途中にNoneがあったらエラーを投げる
result: int = stb.get(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n",
    int,
)
# 途中にNoneがあったらNoneを返す
result: int | None = stb.get_or_none(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n",
    int,
)
# 途中にNoneがあったら、インスタンスを作っていく
result: int = stb.ensure_get(
    "stb_model.stb_sections.stb_sec_beam_rc[0].stb_sec_bar_arrangement_beam_rc[0].stb_sec_bar_simple.stb_sec_bar_simple_main[0].n",
    int,
)
~~~
mypyについては使えるようになり、 vscodeの補完を見れば型情報もわかります。
とはいえ、型情報の指定はやや面倒です。
また、最初にコードを書くときは良いのですが、文字列アクセスに頼るので保守性が下がり、安定性にかけます。
ST-Bridgeのバージョン更新は破壊的変更も生じるため、属性などに変更があった際、実行→エラー確認と繰り返していかないと、問題が生じる箇所を発見できません。

#### 没案3：明らかに欠けない項目はOptionalにしない

必須項目であるidもoptionalはやりすぎではないか、ということで、明らかに欠けない項目(id, 節点や断面参照等)はオプショナルから外すという考え方もあります。

しかし、その場合、どの項目はオプションを外すかが設計判断となってしまうため、採用できません。

#### 没案4：2種類のクラスを持つ

全てがオプショナルであるクラスと、必須項目は通常プロパティで、任意項目だけオプショナルである検証済クラスを作るという案もありました。

概念コード
~~~python
# 全てがオプショナルであるクラス
class RawHoge:
    id: int | None
    foo: str | None
    optional_attr: str | None
    child: RawFuga | None
    optional_child: RawPiyo | None


# 必須項目は通常のプロパティ、任意項目だけオプショナルである検証済クラス
class ValidHoge:
    id: int
    foo: str
    optional_attr: str | None
    child: ValidFuga
    optional_child: ValidPiyo | None


# 検証関数で変換する
def validate(hoge: RawHoge) -> ValidHoge: ...
~~~

これで、必須項目についてはある程度救済されます。

しかし、ST-Bridgeは任意項目だらけであり、特にmypy問題を引き起こしている子要素が軒並みoptionalであるため、
クラスが2倍になる割には、validate後の恩恵が小さいです。

#### 採用案：Standard と or_none のプロパティを2種類作る

概念コード
~~~python
class Foo:
    def __init__(self, *, bar: int | None = None):
        self._bar: int | None = bar

    @property
    def bar(self) -> int:
        if self._bar is None:
            raise NoneAccessError(self._path)
        return self._bar

    @property
    def bar_or_none(self) -> int | None:
        return self._bar
~~~
値はプライベートフィールドに格納します。
通常のプロパティはフィールドがNoneの場合はNoneAccessErrorというカスタム例外を投げ、型ヒントは非オプショナルにします。
これにより、EAFPの記法でmypyエラーが生じません。
NoneはNoneとして取得したい場合もあるので、or_noneプロパティも作ります。こちらはオプショナルな値を返します。LBYL記法はこちらを使います。

この記法により文字列アクセスがゼロにできるので、データモデルが変わった場合などでも静的型チェックで問題点が一覧で即座にわかります。

#### hasattr対応のためのAttributeError継承

新たな問題として、通常プロパティを取れるかどうか確認する際にhasattrが例外で落ちる問題が生じました。
プロパティ化したことで、値がNoneのときに例外を投げるようになりましたが、​Pythonの標準関数 hasattr(obj, "bar") を使うと、内部でプロパティが評価されてしまい、自作のNoneAccessErrorがそのまま外に投げられるためプログラムがクラッシュします。

- 理想と現実のトレードオフ
	- ​理想（言語仕様的）: プロパティ自体は「存在する」のだから、hasattrはTrueを返すべき？
	- ​現実（実務的）:  True が返ってきたのに、アクセスしたら例外で落ちるhasattrは使い勝手が悪い
- 今回の解決策：NoneAccessErrorにAttributeErrorを継承させることとしました

~~~python
class NoneAccessError(AttributeError):
    """Noneへのアクセス時に発生させるが、hasattrを壊さないための例外"""
~~~
hasattrは内部でAttributeErrorだけをキャッチしてFalseを返す仕様になっています。
そのため、NonAccessErrorにAttributeErrorを継承させることで、値がNoneのときは、hasattrがFalseを返す（＝アクセスできないことがわかる） という、実用的な挙動になります。
プロパティが存在するのにhasattrでFalseが返るので、厳密には正しい挙動ではないですが実用性を重視しました。

#### 値の動的型チェック

プロパティ化したことで、代入時のバリデーションもかけられるようになります。
(mypyを使わない人も多いので、静的型チェックのみだと、心配が残ります)
バリデーションのタイミングとしてはセッター時、ゲッター時、両方が考えられます。
一般的な用途ではゲッターが呼び出される回数が圧倒的に多いです。
そのため、セッターでのみ型バリデーションをかけ、フィールドは基本的に正しい値が入っているものとし、ゲッターはチェックなしで取得する方針としました。
これにより、型チェックのオーバーヘッドを減らしつつ、安全にデータを扱えます。

セッターAPI上で間違った値を代入しようとすることはコーディングのミスなので、実行時に即座にエラーを投げます。
一方で、既存のファイルを読み込む際は、誤ったデータがあることもあり、そこでエラーになると利用しにくいです（前述のとおりスキーマ違反のデータも多い）。
そのため、XMLロード時の値格納については別途、違反の値は別の場所に逃がすという処理を行います。

セッター概念コード
~~~python
class Foo:
    @bar.setter
    def bar(self, value: int) -> None:
        if not isinstance(value, int):
            raise TypeError(f"barはintである必要があります:{type(value)}")
        self._bar = value

    @bar_or_none.setter
    def bar_or_none(self, value: int | None) -> None:
        if value is not None and not isinstance(value, int):
            raise TypeError(f"barはintである必要があります:{type(value)}")
        self._bar = value
~~~

### メタクラスによる動的なプロパティ作成

プロパティ設計はできましたが、コードが複雑になります。
コードは自動生成なので、生成は不可能ではないですが、大量のプロパティが並び、クラスが一画面に収まらず見にくいです。
プロパティを簡単に書くだけでも18行であるため、ST-Bridgeの膨大な仕様をこの記法で書くと全部で軽く10万行を超えます。
コードジェネレーターも複雑になり、維持管理も大変になります。

そこで、メタクラスによる自動実装を行います。
各要素のクラスは_fieldsというクラス変数のみをもち、属性情報の辞書をもたせます。
基底クラスにセットしたメタクラスでフィールド情報からプロパティやバリデーション処理を実装させます。

~~~python
class Foo(StBridgeElement):
    _fields: dict[str, FieldInfo] = {
        "bar": FieldInfo(xml_type="integer", kind=FieldKind.ATTRIBUTE, required=True),
        "baz": FieldInfo(kind=FieldKind.ELEMENT, maxOccurs=1),
    }
~~~

※プロパティ実装のみであればdataclassのinit_subclass等メタクラスを使わない手法も考えられますが、後述のslots問題もあるため、メタクラスを採用しています。

### スタブの作成

動的プロパティ作成により実装のコードは読みやすくなるが、動的生成により型ヒントが消滅してしまいます。
最初の目的の型管理をしっかりさせるという目的を実現するために、コードジェネレーターでスタブ(pyi)ファイルも作成します。
プロパティをすべて書くことになりますが、こちらは型だけでよく、実装がないのでシンプルに記載できます
~~~python
class Foo(StBridgeElement):
    def __init__(self, *, bar: int | None = ..., baz: Baz | None = ...): ...

    bar: int
    bar_or_none: int | None
    baz: Baz
    baz_or_none: Baz | None
~~~
これで静的型チェックやIDE補完も問題なくなります

### ensureの実装

ensure_getの実装を考えます。さらにbar_or_crateメソッドを作ると、ただでさえ1つの属性に2つプロパティがあって補完が長いのに、3行ずつになると鬱陶しいです。
ensure_barメソッドにすると、並び順では一箇所に固まりますが、それなりに邪魔です。eは降順で前の方なのもよくないです。

また、これまでのプロパティは値を取得するだけですが、ensureは操作がはいるので、同列には扱わない方がよいと考えます。
そのため、_EnsureAccessorBaseという薄いラッパーを作り、プロパティ名が無ければインスタンスを作って返すプロパティを定義します。
~~~python
class _EnsureAccessorBase:
    def __init__(self, obj: StBridgeElement):
        self._obj = obj

    def __getattr__(self, attr):
        # objに指定したプロパティがあったら、Noneじゃなければそのまま返し、Noneならインスタンスを作ってから返す関数を返す
        ...


class StBridgeElement:
    @property
    def ensure(self):
        return StBridgeEnsure(self)
~~~
型補完を効かせたいので、スタブを工夫して、各要素ごとにラッパーがあるように見せかけます。
スタブ上のクラスと実際のクラスが異なるので、エラー時に追いにくいというリスクはありますが、実用性を優先しています。

スタブのイメージ
~~~python
class Foo(StBridgeElement):
    def __init__(self, *, bar: int | None = ..., baz: Baz | None = ...): ...

    bar: int
    bar_or_none: int | None
    baz: Baz
    baz_or_none: Baz | None
    ensure: FooEnsure


class FooEnsure:
    def baz(self) -> Baz: ...
~~~

インスタンスを作る、という加工を行うため、インスタンス取得はあえてプロパティではなく関数を採用しています。

### 拡張、不正値のロード

python上でデータモデルを扱うときはバリデーションにより安全にモデルを作成時は利用できるようになりました。
しかし、前述の通り、流通しているデータは拡張やスキーマ違反が含まれます。
ST-Bridgeは、モデル内で拡張ルールを定義することにより、仕様で定められていないの属性や要素を利用してもよいという事情もあります。
一方でちゃんとルールを定義してない仕様違反の拡張も流通しています。
pythonはクラスに自由に属性を追加できるため、やろうとすれば仕様違反のものをそのまま属性として追加することはできます。
しかし、安定性にかけるし、slotsを使いたい事情もあります。
そこで、基底クラスに拡張情報を格納するクラスを持たせます。
通常はNoneにしておくことでメモリはあまり消費しないで済みます。
~~~python
class _ExtensionData:
    def __init__(self):
        self.attributes: dict[str, Any] | None = None
        self.children: dict[str, list[StBridgeElement]] | None = None
        self.invalid_values: list[_InvalidValue] | None = None


class StBridgeElement:
    __slots__ = ["extension"]

    def __init__(self):
        self.extension: _ExtensionData | None = None
~~~
ロード時にスキーマにない属性や子要素があったら、StBridgeExtensionのインタンスを作り、いったんそこに格納します。
ロード終了時に、モデルの拡張定義を確認し、定義違反のものがあればinvalid_valuesにデータを移します。
スキーマにある属性だが型違反等の属性もinvalid_valuesに格納します。
invalid_valuesは値の他、違反理由、XPathも入れることでデバッグしやすくなります。

※拡張、不正値については機能は実装したものの、現在APIが固まっていないため、まだプライベートな属性となっています。
今後利用するためのAPIを固めていきたいと考えています。

### メタクラスを採用した理由

メタクラスは黒魔術とも呼ばれ、一般的に使わないで済む場合は使うべきではないと言われています。
今回メタクラスを採用したのはslotsの採用を行うためです。
もともとはdataclassで実装していましたが、前述の通り、サブクラスは最低限の実装とし、基底クラスを充実させます。
そのため、_extension, _parent_ref等、基底クラスもフィールドを持ちます。
現状のdataclassはslots=Trueが使えますが、slots=Trueのクラスを継承させるとうまく継承が行えません。
slotsはクラス生成時に確定するため、サブクラスでのslots統合を行うためにはクラス生成時の挙動を変えられるメタクラスの採用が必要でした。

### parentの保持

今後執筆予定

### AI時代のメリット
このライブラリは人がコードを書くことを想定して人の手で設計しましたが、近年はAIエージェントの発達により、人がコードを書く時代ではなくなってきました。
しかし、静的な型付けが厳密にされていたり、セッターのバリデーションやパス付きのNoneAccessErrorで、エージェントの試行錯誤はしやすくなると考えられます。

## 複雑性の隠蔽

上記の設計の通り、コア部分は非常に複雑な設計となっています。

STB-KITのコア部分のメンテナンスは難易度が高く、バス係数が小さくなる部分はあると思います。

しかし、このコア設計により利用側のコードはシンプルに書くことができます。

STB-KITは仕様自体が非常に複雑であるため、どこかには複雑なコードを記載する必要があります。

そこで、stbkit-core側に複雑さを押し込め、シンプルなAPIを用意することで、
stbkit側のIFCコンバートやアップグレードなどの設計判断を伴うコードが読みやすくなります。

コア部分は一度作れば頻繁にメンテする必要はありませんが、コンバートやアップグレードは今後もメンテナンスが続きます。

そのため、このような設計となりました。

とはいえ、複雑すぎるとも思うので、APIを簡略に保ちながらできる内部の簡略化は今後検討していきたいと思います。

是非皆様の知見をIssueやDiscussionへ投稿いただけると助かります。

現状では、STB-KITのメンテナンス自体のハードルはやはり高くなっているため、できるだけ経緯などを残すためにこのドキュメントを作成しました。

現在のAIエージェントの発展を見ていると、将来的にはメンテ時にAIにこのドキュメントをコンテキストとして渡すことで、メンテ負荷が減ると考えられます。

## IO

入出力はjsonやpickleなどの利用者も使いやすいように、load,loads,dump,dumpsを採用しています。

方針は、入力は寛容に、出力は厳しくとしています。

ST-Bridgeはスキーマ違反のデータも多いことから、入力時はできる限り不整値も診断できるようにデータとして保持します。

一方で、スキーマ違反のデータの流通をSTB-KITが増やさないように、出力時はスキーマ検査を行い、違反したものは出力できない仕組みとしています。

### pydanticの採用を見送った理由

データバリデーションライブラリはいろいろあり、よく使われるのにはpydanticがあります。

WEBのAPIデータなどであれば迷わずこちらを使うと思います。

これは、APIデータなどは下記のような特徴があるからです。

- 基本はスキーマにあったデータがくる
- 扱う量が多い
- 間違ったデータがあれば400 bad requestではじく。リクエストを送る側がスキーマに合わせるべき

一方で建築、ST-Bridgeの特徴は下記のようになります。

- 仕様が複雑であるため、スキーマ通りのデータを作るだけでもハードルが高い
- 建築は基本的に一品生産。設計中は1つのデータと深く向き合う
- データが間違っているからと言って、拒否すると設計に利用できない

実務では、読み込みエラーが出たデータは結局XMLを探し、修正したり拾いに行ったりなどが行われます。

そのため、STB-KITでは、不正な値があってもとにかく読み込み、不整値も診断し必要であればスクリプト処理できることを考えています。

このような処理を行おうとすると、カスタムバリデータだらけになってしまい、それであれば最初から自作クラスでもよいのでは?となりました。

また、dataclassと同様、継承先のslots問題もあり、pydanticの採用は見送りました。

### ストリーミング対応

開発初期は下記のような処理を行っていました。

~~~
XMLを文字列に展開 -> ElementTreeにすべて展開 -> dictに展開 -> データモデルを読み込み
~~~

しかし、何回もデータ展開を行っているため、メモリをかなり多く消費してしまう状態でした。

読み込みは書き込みに比べ、頻度も多くなるため、XMLファイルからデータモデルへ直接ストリーミング読み込みさせるように改良しました。

具体的には、ElementTreeのXMLParserにtargetを渡し、イベント時にST-Bridgeのデータモデルを直接モデルを組み立てています。
これにより中間データを作らずに済みます。

出力についても、今後ストリーミング化を検討したいです。

## コンバーターの方針

### 最新モデルからの変換のみとする

ST-Bridgeはバージョンにより部材表現が変わる場合があります。
IFC等他形式への変換をバージョンごとに実装するのはメンテナンス負荷が高まります。
そのため、他形式への変換は常に最新バージョンからのみとし、ST-Bridgeのバージョンを最新版に上げる処理を実装することにより、各バージョンの変換に対応します。

### 現状は外形のみ

現時点ではバージョンアップおよびIFC等他形式への変換は外形形状のみとしています。（配筋や細かい属性は除く）
これは、IFC等他形式が必要な状況は、主として部材サイズが一致しているか、干渉していないか等のチェックとなり、外形形状があることで比較検討ができることが大半だからです。
配筋の比較などはBIMと計算プログラムの比較などになりますが、構造設計内の整合確認であればST-Bridge同士で比較できます。
このことから、ST-Bridgeの属性全てを他形式に変換して持たせるのは、負荷が高いわりに需要が小さいと思われるため、外形形状のみに限定しました。
これはまずは安定化を優先しているためであり、将来的に細かい属性も含めた変換を進めていくことを排除するものではありません。

### IFCへの変換方針

変換を外形だけとしているため、IFCファイルを見ているときに、ある部材の配筋などの詳細情報を確認したくなったらST-Bridgeファイルを見る必要があります。
そのため、IFCの部材のエンティティとST-Bridgeの要素を対応させ、GlobalIdとguidを一致させます。
これによりIFCのエンティティに対応するST-Bridge情報を取得できるようになります。

上記の方針からSRC部材は初期設計では外形のRCのみの変換としていましたが、SRCでS部分が見れないのは実用的ではないという意見があったため、IfcElementAssemblyを利用し、そこにS、RCのエンティティを所属させました。
AssemblyのGlobalIdがST-Bridgeのguidと一致するため、SRC部材についても対応を追うことができます。

その他詳細については今後執筆予定

## テストの方針

このプログラムは実務モデルでの変換を目指しているため、ゴールデンテストを重視します。
GitHub上のテストフィクスチャはテスト用に作成した小さいモデルを中心に設置しています。
一方で、重要なことは実務で利用する計算ソフトから出力されたモデルが正しく扱え、変換できるかどうかです。
しかし、実プロジェクトのモデルなどは権利関係やセキュリティの問題で、OSSに入れることはできません。

そこで、公開データの他、非公開のテストモデルもスムーズにCIでテストできる仕組みとしています。
具体的にはfixtureリポジトリというフィクスチャ管理クラスを作り、fixtureを設置するパスを指定できるようにします。
フォルダの中のcase.ymlというファイルに、実行するテストの仕様を記載することで、動的にgoldenテストが実行できます。
パスの指定はテスト実行時のcliオプションで指定します。
この仕組みにより、ソースコードを変えることなく、各社が社外秘のモデルを使用して、CIテスト(今回はラウンドトリップテストと、フォーマット変換が対象)を回せるようになり、実用的な品質チェックが行えます。
社内モデルのゴールデンテストで問題が生じた場合は、その部分の回帰テストを公開可能な小さいゴールデンやユニットテストを追加していくことにより、知見が増えていくと考えられます。

### diffの設計
goldenテストを重視するにあたり、ゴールデンテストでエラーになった場合に問題を追うために、ST-Bridgeのdiffを取れるようにしました。
これはあくまで開発者が差分を検出するためのもので、実務におけるモデルの整合性確認とは異なる概念となります。
実務における差分はSTB-DiffCheckerという強力なアプリケーションがあるため、STB-KITでは扱う予定はありません。
ST-Bridgeをインデント込みで出力すれば、テキストdiffである程度の差分はわかりますが

- 出力app_name,app_version等、比較する必要のないものもある
- guid等も比較対象から除きたい場合がある
- インデント、改行など、フォーマットの差分については考える必要はない。

等を考えると、やはり構造化したデータとして比較したいです。
そこで、ST-Bridgeのモデルとしてではなく、ST-BridgeをXMLとして比較するユーティリティを作成しました。
上記の要件を考慮するほか、差分をわかりやすくするため、比較対象をid一致で判断するなどの機能も入れます。
基本的にはゴールデンテスト用の機能ですが、stbkit以外の開発者がdiffをとりたいシチュエーションもあるかもしれないので、testutilsではなくstbkit本体に入れ、cliから実行できるようにしました。

### IFCファイルのgoldenテスト

IFCファイルは、ソースコードを変えていなくても、モデルによってはIFCのgoldenテストが失敗する場合がありました。出力が実行ごとに変わってしまうためです。

IFCファイルはstep形式のファイルですが、出力のたびに微妙にエンティティの並び順が変わってしまうことが判明しました。

意味としては同じファイルのはずですが、ファイルは一致しないため、重視しているgoldenテストで結果を確認できません。

ST-Bridgeについては自分で作成しているため出力が安定しますが、
IFCファイルはエクスポートをifcopenshellに任せているため、意図しない部分で出力が安定しない部分がありました。

そこでIFCの出力結果を確認できるように試行錯誤を行いました。

#### mockによる乱数値の固定化

IFCはすべてのentityがGlobalIdを持ちます。指定しないでエンティティを作成するとランダムなidがふられます。

そのため、GlobalIdについてはSTB-KIT側でuuidにより生成し、必ず指定してentityを作成します。

テスト時はmockのpatchによりuuidの生成関数を置き換えて値を固定化します。

完全な固定値だとGlobalIdの指定ミスに気付けないため、patchにカウンターをつけて、一意なuuidになるようにします。

これでGlobalIdは安定します。

また、IFCの出力にはファイル作成日時も記録されます。こちらもfreezegunで固定します。

出力の表示自体はだいぶ安定しましたが、それでもエンティティの並び順はやはり出力ごとに毎回変わってしまいます。

#### PLYによる確認

当初はIFCファイル自体のgolden比較はあきらめ、ジオメトリにより確認する手法をとりました。
（前述の通り、外形形状の変換を主眼に置いているため）

IFCからPLYを生成する仕組みを作ったため、そのPLYのメッシュを比較することにより出力のチェックを行いました。

IFCの出力順がランダムのため、PLYのメッシュの出力順もランダムとなるため、こちらもファイルの単純比較はできません。

そこで、バウンディングボックス、表面積、体積、座標を許容誤差で丸めて並べ替えた頂点リストを比較することで、出力がずれたら検出できるようにしました。

#### エンティティ並びの固定化

しばらくはPLY比較のみでやっていましたが、やはり本丸のIFCを直接確認したいです。

AIの性能が上がってきたため、AIエージェントに状況分析をさせたところ、原因が判明しました。

当初は、ifcopenshellのassign_container/assign_objectにより要素の親子関係を指定していました。

AI分析により、下記の結果が得られました。

> 上記APIは呼ぶたびに既存要素との集合演算と要素ごとのplacement作り直しを行う。
> このときエンティティをsetへ入れており、entity_instanceのハッシュにはメモリアドレス(file_pointer)が含まれるため、並びが実行ごとに変わっていた。
> create_entity時にRelatedObjectsやRelatedElementsで入力順のリストを直接渡すことで決定的になる。

実際にその通りで、entityの並び順を固定化することができました。

なお、上記対応は副産物も大きかったです。
assign_xxxは集合演算が毎回走るため非常に重く、O(N^2.5)程度のオーダーでした。
そのため、10万要素レベルの大規模モデルだとIFC出力に6時間かかったりしていました。
それが、この変更によりO(N)になったため、6時間かかっていた変換が数分で済むようになりました。

#### 単位並び順の固定

上記対応でほとんどファイルは固定されていましたが、単位の行のみずれる場合がありました。

こちらもAI分析させたところassign_unitもsetを経由するため、並び順が安定しないとのことでした。

こちらは生成後にソートすることにしました。

これで、ついにIFCの出力が固定化され、開発環境でgolden比較をしてもテストが通るようになりました。

なお、PLY比較はPLYファイル自体のテストも必要であるため、引き続き実施しています。


#### 順序依存の解消

通常実行では安定したと思いましたが、テストを全件流した時は成功するのに、1件のみ流すとエラーが生じる場合がありました。

これは、ifcopenshell.guidをインポートすると、その中でuuid.uuid4があるため、uuid利用カウンタが増えてしまうのが原因でした(0始まりになるはずのuuidが1はじまりになっていた)。

これはテスト時に、patchを適用前にifcopenshell.guidをインポートすることでカウンタが増えないようになりました。

#### 実行環境依存の解消

これまでの対応でローカル環境ではテストがスムーズに回るようになりましたが、CIで落ちてしまいました。

結果を見ると、WindowsとLinuxのPython環境の差で丸め誤差が生じているようでした。

そこで、floatに関してはifcopenshellに数値を渡す前に丸める仕組みを作り、テスト時のみ数値を丸めるようにしました。

これにより、OS間でも数値が安定するようになりました。

最後に1行、また日付の部分がずれていました。これはタイムゾーンがローカルはAsia/Tokyoで、CIではUTCになっているようでした。

monkeypatchでうまく調整したかったですが、うまくいかなかったので、いったんシンプルに文字列置き換えでタイムゾーンを補正しています。

これはローカル環境のタイムゾーンが東京かUTCでない地域だとエラーになってしまうため、タイムゾーン固定方法の知見がありましたらIssueで教えていただけると助かります。

これで、ローカル、CIともに確定的なIFCファイルを出力できるようになりました。

## GUIアプリについて(今後公開予定)

stbkitでST-Bridgeをソフトウェアから利用できるものの、利用するにあたりデバッグ用に正しいST-Bridgeになっているか等を確認したり、テストモデルを人の手で自由に編集できることが望ましいです。
そこで、stbkitにも3dビューワー及び、guiによるST-Bridgeの編集機能をつけています。
ただし、このツールはST-Bridgeツール開発者向けであり、実務で利用するBIMオーサリングツールを目指すわけではありません。
そのため、機能は開発で欲しくなる最低限とし、シンプルなものとします。

### rustの採用

3dビューワーとguiを実装するにあたっては、依存なしは非効率であるのでサードパーティライブラリを利用します。

pythonは実行速度が問題になりがちですが、3dライブラリなどは実装はc++であることがほとんどなので、速度の問題はないと考えられます。

pythonで簡単に3dビューワーを作るにはpyvista等が手軽で使いやすいです。
ST-Bridgeの、編集画面は標準のtkでも十分実装できますが、連動させようとするとvtkと相性が悪く、プロセス間通信やインタラクティブ無効等が必要で、実装が複雑になります。
guiにpysideを使えば、pyvistaと連動したアプリケーションをきれいに作れます。
しかし、pyvistaとpysideを依存に含めると、容量が数百メガバイト必要となり、ビルドエラーも起きやすいです。
軽量さを目指すstbkitの目標からやや離れてしまいます。

そこで、ビューワーとguiエディタはrustで実装することで、軽量に3dビューワーとGUIエディタを利用できるようにしました。
また、Wheelにビルド済バイナリを同梱することで、インストールも安定し、容量も非常に小さくなります。

stbkitの、コンセプトで人の目が通りやすいpythonを採用するとしましたが、これはデータ変換など重要かつ長期的に安定が求められる部分のことです。
3dやguiは、一時的に使うものでデータとして残るわけではなく、また時代により使われるツールは変遷していくことから、現時点で理想的な環境としてrust拡張を採用しました。

rust拡張はstbkit-nativeとしてパッケージを完全分離し、コントリビューターはrustを知らなくても貢献できるようにします。

先程のシンプルな実装で良いという目的と、viewerとguiの連動のしやすさから、egui及びthree-dを採用しています。
軽量化を目指して採用した組み合わせですが、副次的効果として両方ともwasm対応のため、アプリをブラウザでも動かせるようになりました。

### GUIの設計

今後執筆予定

## ライセンスの選定

本プロジェクトはMPL-2.0を採用しています。

ST-Bridgeは公共性が高いため、改善フィードバックを求めたい一方、GPLなどのコピーレフトのライセンスにすると計算プログラムなどに組み込めません。

そこで、ifcopenshellを参考に、準コピーレフトライセンスを採用することとしました。

一方で、STB-KITは一部で静的リンクが基本のRustを採用しているため、動的リンクを求めるLGPLとは相性が悪いです。

そこで、ファイル単位のコピーレフトであるMPL-2.0を採用しました。

## 開発のAI利用方針(2026/9時点)

本プロジェクトはAIエージェントの利用は禁止していません。

開発時はエージェントがなかったことから、初期実装はすべて手書きですが、
リファクタリング、処理の効率化、ドキュメントの整備、バグや脆弱性検出などでAIエージェントの利用を始めています。

しかし、ドメイン知識が多く必要になるため、下記の方針で利用していく予定です。

原則として、AIエージェントは実装や検証の補助にのみ利用し、ST-Bridgeや構造設計に関するドメイン上の決定を行う主体とはしません。

### 原則AIエージェントを利用しないもの

- コア部分の設計（AIとの意見交換は積極的に行う）
- ドメイン知識が重要なST-Bridgeのバージョンアップ実装やIFCへの変換の初期実装
- goldenテストモデルの作成

### 人の重点的なチェックを前提に、AIエージェントを補助的に利用するもの

- 実装を行い、テストで結果を固定したうえでのリファクタリング
- 細かい分岐用のユニットテストの作成

### 積極的にAIエージェントを利用していくもの

- Pythonの実装変更をrust, JavaScript, ドキュメントなどの対応部分に同期
- データに影響しない3Dビューワーやguiの細かい設定等
- バグや脆弱性の検出（修正の妥当性判断は人が行う）
- ユーザードキュメント整備

Copyright 2026 TAISEI CORPORATION
